<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Действие: открыть только заказы с экспресс-доставкой -->
        <record id="action_express_orders" model="ir.actions.act_window">
            <field name="name">Express Orders</field>
            <field name="res_model">sale.order</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('order_line.is_express', '=', True)]</field>
        </record>

        <!-- Действие: отметить все линии как экспресс -->
        <record id="action_mark_express" model="ir.actions.server">
            <field name="name">Mark All Lines as Express</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="state">code</field>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_type">action</field>
            <field name="code">
for order in records:
    order.order_line.write({'is_express': True})
            </field>
        </record>

        <!-- Действие: показать связанные экспресс-пикинги -->
        <record id="action_express_pickings" model="ir.actions.act_window">
            <field name="name">Express Pickings</field>
            <field name="res_model">stock.picking</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('origin', 'ilike', '(Express)')]</field>
        </record>

    </data>
</odoo>
